language: java
sudo: false
script: mvn clean install
jdk:
  - oraclejdk8
services:
  - mongodb
before_script:
  - sleep 15
  - mongo test --eval 'printjson(db.getCollectionNames())'
notifications:
  email: false
